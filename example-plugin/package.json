{
  "name": "example-plugin",
  "version": "1.0.0",
  "description": "Example plugin demonstrating the build workflow",
  "main": "dist/main.js",
  "scripts": {
    "build": "node esbuild.config.js",
    "build:prod": "NODE_ENV=production node esbuild.config.js"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "esbuild-sass-plugin": "^2.16.0",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "plugin",
    "esbuild",
    "example"
  ],
  "license": "MIT",
  "dependencies": {
    "obsidian": "^1.8.7"
  }
}
